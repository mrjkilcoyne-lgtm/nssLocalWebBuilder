# REFERRED QA Report — 2026-04-03

## Summary

| Metric | Value |
|--------|-------|
| Companies | 93 (3 orphaned) |
| Products | 211 |
| Affiliate Links | 68 |
| Products WITH links | 68 (32%) |
| Products WITHOUT links | 143 (68%) |
| Amazon ASINs | 59 |
| No descriptions | 0 |
| No regions | 0 |
| Price inverted | 0 |
| Active deal ticker | 20 |
| Active easter eggs | 4 |

---

## P0 — Site Breaking

| # | Issue | Location | Detail |
|---|-------|----------|--------|
| 1 | **Search bar does nothing** | `useCatalog.ts` | The search input accepts text but the hook never sends search_query to Supabase. Typing in search shows 0 results or no change. The old RPC call used wrong param name (`search_term` vs `search_query`), and the rewrite removed search entirely. |

## P1 — Feature Broken

| # | Issue | Location | Detail |
|---|-------|----------|--------|
| 2 | **Category filter doesn't work server-side** | `useCatalog.ts` | Categories are filtered client-side AFTER pagination, so filtered count is wrong. If page has 12 items but only 3 match the category, you see 3 items but "Showing 12 of 208". |
| 3 | **68% of products have no affiliate link** | Supabase data | 143 products have no affiliate_links rows. "Get Deal" on these goes to product detail page which also has no deal to show. |
| 4 | **CSP header missing supabase/google/stripe** | `netlify.toml` | connect-src doesn't list *.supabase.co. img-src doesn't list www.google.com (favicons). Could block requests in strict browsers. |
| 5 | **Buy Me a Coffee link** | `PaymentMethods.tsx` | URL set to `https://buymeacoffee.com/mattkilcoyne` but not wired into the component — still shows "Coming soon" |

## P2 — Cosmetic / UX

| # | Issue | Location | Detail |
|---|-------|----------|--------|
| 6 | **Orphaned companies** | Supabase | Adam Audio, IK Multimedia, PayPal have company records but no products |
| 7 | **Logo images are small favicons** | Supabase logo_url | Google Favicons API returns 128px favicons, not full logos. Looks OK on cards but low-res on product detail pages |
| 8 | **container-page CSS class** | Layout.tsx | Uses `container-page` class but it's not defined in Tailwind config — likely no-op, layout relies on other classes |
| 9 | **TypeScript warning** | `main.tsx:1` | Unused `React` import (removed StrictMode but left import) |

## P3 — Nice to Have

| # | Issue | Location | Detail |
|---|-------|----------|--------|
| 10 | **No favicon** | `index.html` | References `/favicon.svg` but file doesn't exist in public/ |
| 11 | **No meta OG tags** | `index.html` | No Open Graph or Twitter Card meta tags for social sharing |
| 12 | **No sitemap.xml** | Deployment | No sitemap for SEO |
| 13 | **Stack page cosmetic only** | `pages/Stack.tsx` | Shows hardcoded sample data, not connected to real stacks |
| 14 | **Advisor wizard doesn't save results** | `pages/Advisor.tsx` | Recommendations generated but can't be saved/shared as a stack URL |

---

## Fix Priority Order for Dev Agents

### Morning Sprint 1 (P0 + P1)

1. **Fix search** — Add text search to useCatalog using Supabase `.textSearch()` or `.ilike()` on product name + description
2. **Fix category filter** — Move to server-side filter using a subquery or separate products-by-category query
3. **Add Amazon affiliate links for remaining 143 products** — Generate `amazon.co.uk/dp/ASIN?tag=mrjkilcoyne-21` links for all products that have ASINs in the CSV but not in Supabase
4. **Fix CSP header** — Add `*.supabase.co`, `www.google.com`, `*.gstatic.com`, `js.stripe.com`, `buy.stripe.com` to appropriate directives
5. **Wire Buy Me a Coffee** — URL is `buymeacoffee.com/mattkilcoyne`, remove coming-soon state

### Morning Sprint 2 (P2)

6. **Add products for orphaned companies** — Adam Audio T5V, IK Multimedia iLoud, PayPal Business
7. **Fix unused React import** in main.tsx
8. **Add container-page to Tailwind config** or replace with standard Tailwind container

### Afternoon Sprint (P3)

9. **Add favicon.svg** to public/
10. **Add OG meta tags** for social sharing
11. **Generate sitemap.xml** for SEO
12. **Connect Stack page to Advisor output** — save generated stacks to Supabase
